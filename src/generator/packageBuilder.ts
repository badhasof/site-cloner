/**
 * Package builder - generates package.json for output project
 */

export function generatePackageJson(dependencies: string[]): string {
  // Package.json generation placeholder
  return '{}';
}

export function detectDependencies(code: string): string[] {
  // Dependency detection placeholder
  return [];
}
